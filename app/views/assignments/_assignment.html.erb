<%= content_tag_for :li, assignment do %>
  <%= render :partial => 'stock_cards/stock_card', :object => assignment.stock_card, :as => :stock_card, :locals => {:assignment_id => assignment.id } %>
<% end %>